{
  "$schema": "https://turborepo.dev/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["$TURBO_EXTENDS$", "$TURBO_ROOT$/scripts/materialize-marketing-skills.mjs"],
      "outputs": ["$TURBO_EXTENDS$", ".output/**"]
    }
  }
}
